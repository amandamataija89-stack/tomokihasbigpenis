import { Brand, SiteFooter } from "@/components/Brand";

export default function FeedbackThanks() {
  return (
    <main className="wrap">
      <Brand />
      <section className="card stack">
        <h1>Thank you for your feedback</h1>
        <p className="lede">We read every response. Take care, and you&apos;re always welcome to get in touch again.</p>
      </section>
      <SiteFooter />
    </main>
  );
}
