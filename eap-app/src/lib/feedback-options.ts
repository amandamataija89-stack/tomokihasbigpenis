// Answer options for the feedback form. Kept apart from feedback.ts, which is server-only.
export const HELPED = ["Yes, a lot", "Somewhat", "Not really"] as const;
export const RECOMMEND = ["Yes", "Maybe", "No"] as const;
